# Minimal requirements for LOCAL VERIFICATION ONLY
# No GPU training, no ngrok, no heavy dependencies

# Core ML (CPU version is fine)
tensorflow==2.17.0
numpy==1.26.4
opencv-python==4.10.0.84

# API framework
flask==3.0.3
flask-cors==4.0.1

# Utils
pillow==10.4.0