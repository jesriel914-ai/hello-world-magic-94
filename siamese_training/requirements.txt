# Siamese Signature Recognition - Python Backend Requirements
# Optimized for Google Colab T4 GPU with TensorFlow 2.19

# Core ML Framework (already installed in Colab)
# tensorflow==2.19.0

# Flask Web Framework
flask==3.1.2
flask-cors==5.0.0

# Image Processing
# pillow==11.3.0  (already installed)

# Essential packages (most already in Colab)
# numpy==2.0.2
# These are pre-installed in Colab, no need to install:
# - tensorflow
# - numpy
# - pillow
# - pickle (built-in)
# - json (built-in)
# - logging (built-in)